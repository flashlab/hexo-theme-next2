// Header Section
// --------------------------------------------------
.site-brand-container {
  display: flex;
  flex-shrink: 0;
}

.use-motion {
  .column {
    opacity: 0;
  }
  // fork: theme-toggle 按钮随 motion 淡入（配合上游 motion.js 的 pushToSequence）
  .site-brand-container .toggle {
    opacity: 0;
  }
}

@import 'site-meta';
@import 'site-nav';
@import 'menu';

@import 'bookmark';
@import 'github-banner';
