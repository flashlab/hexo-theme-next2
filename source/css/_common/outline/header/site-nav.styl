// fork: 恢复旧版移动端布局（左汉堡+右月亮，搜索/语言在菜单面板内）
.site-nav-toggle {
  display: none;
}

.site-nav-toggle, .site-nav-right {
  +mobile() {
    flex-column();
  }

  .toggle {
    color: var(--text-color);
    padding: 10px;
    width: 22px;

    .toggle-line {
      background: var(--text-color);
      border-radius: 1px;
    }
  }
}

.site-nav {
  +mobile() {
    site-nav-hide-by-default();
  }
}
